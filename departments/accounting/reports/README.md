# Weekly Reports

One file per report, named `<YYYY-MM-DD>.md` (the Monday the report covers through). Generated by the `accountant` subagent per the format in `../CLAUDE.md`. This folder is empty until the first report runs.
