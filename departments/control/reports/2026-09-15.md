# Control/Audit Report — 2026-09-15

Covers 2026-09-15 through 2026-09-21. Prior reports: `2026-08-24.md`, `2026-08-31.md`, `2026-09-07.md`, `2026-09-14.md` (all read directly for continuity).

## Method note (read this first)

No `Agent`/`Task` tool was available this session, so this review was **not** delegated to the `auditor` subagent as `departments/control/CLAUDE.md`'s convention calls for — the function list available was `Read`, `Grep`, `Glob`, `Write`, `Edit` only. This is the same gap every prior report has hit (2026-08-24, 08-31, 09-07, 09-14), so this makes it a fifth consecutive cycle without confirmed delegation. Worth naming as more than an isolated recurrence: this week's accounting report (`departments/accounting/reports/2026-09-21.md`) independently hit the identical gap on a session whose own brief explicitly asserted Agent access "so there's no excuse to skip delegation this time" — the tool was still absent. This is now a board-wide, not department-specific, pattern.

**No `Bash` tool was available either.** Two concrete consequences:
- The code-quality gate (check 6) could not be independently run on `agency-landing`, `fragrance-store`, `fragrance-dupes`, or `products/affiliate-sites/counterscent-producers/`. This week's dashboard task notes (ids 223, 226) self-report `npm run typecheck` clean and behavior verified against the deployed Worker/production database with dozens of passing assertions — consistent with this project's now well-established track record of accurate self-reporting, spot-checked directly in prior reports, but still not independently re-run here.
- I could not run `git log`/`git fetch`/`git branch -r`/`git merge-base` myself. Everything below other than the orphan-branch item was built from reading current file state (`HANDOFF.md`, dashboard files, `CONSOLE-PLAN.md`/`CONSOLE-REDESIGN.md`/`CONSOLE-REDESIGN-TASKS.md`, `shared/clients.md`, `ledger.md`, this week's accounting report, and the shipped code in `products/affiliate-sites/counterscent-producers/src/`), not a diff against last week.

**The orphan-branch item below was supplied directly by the top-level session that commissioned this audit**, which does have git access this session; it is relayed here rather than independently re-derived, per that session's instruction, and it corrects something this department itself got wrong last week (see finding 1).

## Coordination breaks

### 1. The orphaned remote branch was reported "resolved" last week. It was not, and the wrong resolution is itself a lesson in exactly the relay risk this department exists to catch.

`git branch -r` shows `origin/claude/department-collaboration-view-iqy07o` still exists, unmerged. `git merge-base main origin/claude/department-collaboration-view-iqy07o` fails with **"no merge base"** — this is a disconnected history, not a branch a few commits behind `main`. Its 5 commits are all dated 2026-08-19 and are the earliest scaffold of this entire workspace (initial department/subagent scaffold through the first `agency-landing` template). Despite the branch's name, none of its content relates to a "department collaboration view" — the name looks like a leftover from an unrelated or misnamed session, and the most likely explanation is a stray branch from a repo re-initialization, not live unmerged work.

The reason this needs stating plainly: **the 2026-09-14 report closed this out as resolved**, citing `HANDOFF.md`'s own account that the branch "was checked 2026-09-07 and confirmed empty relative to `main` — safe to ignore or delete," and explicitly flagged that this department itself had not independently re-verified it with `git fetch`/`git merge-base` (no Bash that session either). That closure was premature. The branch is not "empty relative to main" — it shares no history with `main` at all, which is a materially different fact than the one that was reported as settled. Neither the 2026-08-24, 2026-09-07, nor 2026-09-14 reports ever ran the actual git commands; each relayed either the founder's or `HANDOFF.md`'s prose. This is precisely the "verify a subagent's/document's claimed state yourself" rule the root `CLAUDE.md` states generally, applied here to a claim this department itself passed along uncritically.

Per this department's read-only, no-git-write charter: **no action taken on the branch** (no merge, no delete, no checkout). Flagged for the founder to look at and decide — most likely a stray branch worth deleting, but that is the founder's call, not this department's.

### 2. `HANDOFF.md` — the document whose own stated job is "what state the work is in" — is now roughly three days stale against both the dashboard and the shipped code.

`HANDOFF.md`'s most recent dated section is headed **2026-09-18**, ending with "What is next, and it is one thing: The review queue." But the dashboard's own task list (`internal/dashboard/design/Main.dc.html` and `sirketim-dashboard.html`, both consistent through task **226**) documents substantial, real, fulfilled work dated **2026-09-19 and 2026-09-20** that never made it into `HANDOFF.md`: self-serve company creation for producers (task 217, 2026-09-19 — closing exactly the gap `HANDOFF.md`'s own 09-18 entry didn't yet know about), Google/Microsoft OAuth built and Microsoft deliberately dropped the same day (218), a rebuilt sign-in card (219), the account-auto-link reversal (220), a nav/logo fix (221), a sign-in desktop-layout fix (222), a pre-payment audit that found and fixed two real blockers including discovering a second Neon database split beyond the one already documented (223), three further audit findings closed including an append-only audit-log gap (224), the Paddle-to-Payoneer payment-rail decision (225), and a catalogue "Phase 4" closeout (226). Verified directly against the code, not just the dashboard prose: `products/affiliate-sites/counterscent-producers/src/routes/company.ts` exists and is wired into `src/index.ts`'s `ROUTES` table at `/console/company`, exactly matching task 217's claim.

This is the mirror image of most previous reports' dashboard-lag findings — this week the **dashboard is the more current artifact**, and the narrative handoff document has fallen behind it. `HANDOFF.md` explicitly calls itself "perishable... not a permanent document," so this isn't a surprise failure mode, but a reader opening it today for "what state is the work in" gets a materially incomplete picture.

### 3. The producer-console redesign that directly answers the founder's own stated UI complaint remains unbuilt three days after being fully planned, while other work shipped instead — not a silent drop, but undocumented as a deliberate reprioritization.

`CONSOLE-REDESIGN.md` and `CONSOLE-REDESIGN-TASKS.md` (both 2026-09-18) are a careful, founder-quoted response to a real complaint ("too much information at once... user need to go bottom of the page for routing... i want to see a useful end result"). Verified directly against the shipped code that phase 1 (`W1`–`W12`: the `/` vs `/console` split, progressive disclosure, `/standards`) has **not** landed:
- `src/index.ts`'s `ROUTES` table has no `/standards` entry (task W6).
- `producerConsole()` in `src/routes/console.ts` still renders a full signed-out explainer rather than redirecting to `/sign-in` (task W3) — the two-copies-of-the-same-argument problem the redesign doc opens with is still live.
- `overview.ts`'s public status pill still reads **"Not open yet"** with the note **"there is still no way to submit anything"** — `/console/submit` has been live since 2026-09-16 (5 days). This exact staleness was already self-diagnosed as a known instance in `CONSOLE-REDESIGN-TASKS.md`'s own C4 "stale-claim sweep" task three days ago, and it is still live on a public page today.

To be fair to what did happen: the app-shell nav (section 2.2, "SHIPPED 2026-09-18") is real and verified in `src/ui/layout.ts` — the "have to scroll to the footer to navigate" half of the complaint is fixed. The dashboard also tracks the rest honestly (task 203 "assigned", task 205 "blocked" on founder decisions), so this is not a case of work quietly disappearing. But three days and a very substantial amount of engineering effort (OAuth, the payment-rail decision, a full pre-payment audit) went into the phase-2-adjacent backend track instead of the phase-1 UI work the founder actually asked for first, with no note anywhere recording that as a deliberate call. Worth an explicit founder check-in on sequencing.

### 4. The comparison/review content unblock still has no recorded handoff — now open across every report since 2026-08-24, roughly a month.

`Glob products/affiliate-sites/fragrance-dupes/app/comparison/**` and `.../app/review/**` both still return zero files; `content/` (re-confirmed via `Glob`) still holds only `guide/*.mdx`, `schema.ts`, `loader.ts` — no `comparison/` or `review/` subfolder. `departments/content/` itself (re-confirmed via `Glob`) contains only `CLAUDE.md` — no `projects/` folder exists yet even for the newly-assigned console-redesign copy work (task 204), consistent with finding 3's read that content's phase-1 tasks (C1–C4) haven't started either. 77+ real tracking links across three dupe merchants remain unused for this content type.

## Hygiene gaps

### 5. `shared/clients.md`'s COUNTERSCENT Producer Program row is now more stale than at any prior report.

It still reads "Founder to settle the revenue model (`PRODUCER-PROGRAM.md` §2) so the tier/close scene can be written." The revenue model was settled 2026-09-10; pricing has since been revised twice more (per `FINALIZATION-GUIDE.md`, referenced in dashboard task 206/214 notes); the payment-rail decision (Paddle → Payoneer) was made 2026-09-20; and the actual producer console — accounts, sign-in, submissions, withdrawal, an admin approval queue — is live in production. The registry row describes a pre-decision, pre-build state of a product that is now substantially built. First flagged 2026-09-14 as stale; the gap has widened every week since.

### 6. `shared/clients.md`'s Milena Dranka row — unfixed since first flagged 2026-08-24, now a full month.

Still reads "posting cadence set at 2 posts/week Instagram + 2 posts/week TikTok" (missing the Fanvue expansion made 2026-08-22) and "Founder to supply identity reference... nothing generated yet" (resolved 2026-08-22 per that department's own `brief.md`). Confirmed unchanged via direct `Read` this session — identical text to the 2026-09-14 report's quote.

### 7. `departments/architecture/clients/377-1/CLAUDE.md` and `shared/clients.md`'s 377/1 row — unfixed since first flagged 2026-08-24, now a full month.

Re-confirmed via `Glob`: the folder still contains only `CLAUDE.md`, `brief.md`, `notes/`, `scripts/` — no `cad/` folder. The project's own `CLAUDE.md` still asserts "full production set... is now built AND verified," still cites the deleted `377-1-consolidated.dwg`, still claims "39/39 checks pass," with no mention that the deliverable was reviewed, rejected by the founder in person, and deleted (commit `d4db605`, 2026-08-21). This is the single longest-open, most serious hygiene item this department has tracked.

### 8. Dashboard `realReport` wiring for Control and Accounting — unfixed since first flagged 2026-08-31, and the published dashboard's Control card now states an actively false, security-relevant claim.

`internal/dashboard/design/Main.dc.html`: the Control card (`id: 'ctrl'`, line 558) still has no `realReport`, only `demoReport: 'Aug 18'`. The Accounting card (line 555–557) still points to `{ label: 'Finalization Guide · Sep 18', ... }` rather than any of Accounting's own five weekly reports. The published `sirketim-dashboard.html` carries the same gap (`ctrl` `report: null` at line 845; `acct` still the Finalization Guide at line 843).

**New and more serious this week**: `sirketim-dashboard.html`'s Control card `activity` string (line 845) reads *"Standing checks — `/review` is public and inert, which is safe only while it stays inert."* That was true when written but has been **false since 2026-09-18** — `/review` has required `requireAdmin` (an `ADMIN_EMAILS` allowlist) since that date, confirmed directly in `products/affiliate-sites/counterscent-producers/src/routes/review.ts` and `src/lib/admin.ts`. The one department card asserting a security fact on the founder-facing dashboard is itself the one carrying the stale, false version of that fact.

I could not check whether the *published* Artifact was re-seeded after this repo state (no `Artifact`-tool access this session, same limitation as every prior report) — flagging the gap in my own verification rather than asserting either way, per this check's own convention.

### 9. Advertising's `fragrance-store-quality-upgrade` campaign doc — unfixed since first flagged 2026-08-24, now a full month.

`assets/manifest.md` line 6 still reads "**Not yet downloaded to local disk**"; re-confirmed via `Glob` that all 18 images are live under `products/web-templates/fragrance-store/public/`. Lowest-priority item on this list, but the longest-unfixed alongside findings 6–7.

### 10. A stale doc-comment inside shipped code, contradicted by the same file a hundred lines later.

`products/affiliate-sites/counterscent-producers/src/index.ts`'s own header docblock (lines 16–18) states: *"What is still exactly a skeleton: `/review`, which stays unwired because it needs an access-control story first - there is no role column on `User` and it is currently linked from a public page."* The same file's `ROUTES` table (lines 205–209) shows `/review` **is** wired, and its own inline comment there says *"Behind `requireAdmin` since 2026-09-18."* `src/routes/review.ts`'s header separately confirms the same. The "no role column" half is still accurate (the founder deliberately chose an env allowlist over a schema column, per `src/lib/admin.ts`), but "stays unwired" and "currently linked from a public page" are both false as of this file's own later content — the public `/` overview page's card for the review queue no longer carries a link (confirmed in `src/routes/overview.ts`). Low-severity, but a documented instance of the root `CLAUDE.md`'s "a stated reason outliving its cause" pattern, inside a single file.

## Output gaps

### 11. Sales and Content remain idle beyond tasks already assigned but not started.

`departments/sales/pipeline/` and `departments/sales/proposals/` still hold only `.gitkeep` (confirmed via `Glob`); dashboard task 173 ("Producer store-direct links") is still `assigned`, unchanged for many weeks. Content's task 204 ("redesign copy, merge map, stale-claim sweep") is `assigned` with no `departments/content/projects/` folder yet created — consistent with finding 3's read that phase 1's content-dependent tasks (C1–C4) haven't started, which in turn blocks the sales review task (S1). The 4 idle web templates from earlier reports remain unaddressed as well; no new go/no-go recorded anywhere.

### 12. Architecture remains idle since dashboard task 132 — now roughly 30 days, up from ~24 last week.

Re-confirmed via `Grep`: no `dept: 'arch'` task exists past 132. Consistent with the documented pause pending the founder's reverse-duplex decision, but finding 7 shows the department's own project file still doesn't distinguish "paused" from "shipped and verified."

## A live, named, still-open security item worth surfacing (not a new Control finding — already in the record, but easy to lose)

Dashboard task 224's own notes end: *"STILL OPEN AND STILL NEEDING THE FOUNDER: rotate the Google client secret, which was pasted into a chat, shipped, and never rotated, and delete the client_secret json sitting on the desktop. Confirm the Google consent screen reads In production rather than Testing."* This is a genuine, live credential-exposure item (a hash comparison in the same task confirmed the production secret is byte-identical to the one pasted in chat weeks earlier and never rotated). It is buried inside a very long, otherwise excellent self-audit note and is exactly the kind of item that reads as "handled" to someone skimming task titles rather than notes. Surfacing it here because nothing else in the dashboard's own status fields (still `fulfilled`) flags that a founder action remains outstanding.

## What's working

- **Web Development / the Counterscent producer console had an outstanding week of self-correction discipline.** The COO's pre-payment audit found two real blockers reproduced live against production (subscription gates reading `tier` and never `status`; approved-but-unscored listings that could never publish) and fixed both with SQL-side date arithmetic that deliberately avoids the timezone-comparison bug this repo has already paid for once. In the same pass it discovered a **second** Neon database split beyond the one already documented (`.dev.vars` vs `.env`/`.env.local` pointing at different endpoints, the deployed Worker reading the latter) — found only because a session row written to the wrong database was rejected by the live site, and reported plainly rather than papered over. A separate pass found and fixed an append-only `AuditEvent` table silently dropping rows for accounts with no producer attached (fixed with a real `NULL`, not a fabricated placeholder row). OAuth was built carefully — Microsoft was evaluated, found to publish no `email_verified` claim, and deliberately dropped the same day it was built, before deploy — and a leaked Google client secret was caught, with `.gitignore` hardened against the whole file class rather than just the one instance.
- **The payment-rail research (Paddle → Payoneer, annual billing) is genuinely rigorous and self-correcting.** It independently caught its own sourced claim that Paddle supports TRY at checkout as false by checking Paddle's own developer docs rather than an aggregated search result, and it reduced the modeled effective cost from ~20% to ~7% through payout-routing and billing-interval choices rather than a pricing change.
- **The dashboard task list itself (`Main.dc.html` and `sirketim-dashboard.html`) is unusually current this week** — both files carry a consistent, detailed task list through id 226, dated as late as 2026-09-20. This is a real improvement over every prior report's dominant finding (a dashboard frozen weeks behind reality); the gap this week has moved to other artifacts (`HANDOFF.md`, the console's own public-facing copy, the published dashboard's per-department activity strings) rather than the task list itself.
- **Accounting's honest "no new ledger rows" reporting continues for a third/fourth consecutive week** despite substantial adjacent product work, and it correctly reasoned through why (Neon's free tier, Paddle's adapter deliberately deferred until a real subscriber) rather than padding the week's numbers.
- **Content's gated posture continues to hold** — `comparison`/`review` remain correctly unpublished, no card anywhere points at a 404.

## Prioritized action list

1. **Rotate the exposed Google OAuth client secret and delete the `client_secret*.json` file from the desktop** (see the security item above) — a live exposure, named as open in the dashboard's own notes, and shouldn't wait for the next redesign or audit cycle to get picked up.
2. **The founder should decide what to do with `origin/claude/department-collaboration-view-iqy07o`** — confirmed this session to be a disconnected, orphaned branch (not "empty relative to main" as last week's report closed it out to be). Most likely safe to delete, but that decision and the delete itself are the founder's, not this department's.
3. **Correct the published dashboard's Control-card activity string**, which currently states `/review` is "public and inert" — false since 2026-09-18 — and add the long-missing `realReport` links for Control and Accounting (unfixed since 2026-08-31).
4. **Bring `HANDOFF.md` current with 2026-09-19/09-20's work**, or explicitly retire it in favor of the dashboard for "current state" purposes — it is billed as exactly that document and is now the more-stale of the two.
5. **Get an explicit founder call on the producer-console redesign's sequencing** — phase 1 (the direct answer to the founder's own UI complaint) is fully planned and untouched three days on, while phase-2-adjacent backend work shipped instead. Not necessarily wrong, but undocumented as a deliberate choice.
6. **Fix the two still-open, already self-diagnosed stale claims on the producer console**: `overview.ts`'s "Not open yet"/"no way to submit anything" status pill (public-facing, 5 days stale) and `index.ts`'s header comment describing `/review` as unwired (internal, contradicted by the same file).
7. **Fix `shared/clients.md`'s COUNTERSCENT and Milena Dranka rows, and `departments/architecture/clients/377-1/CLAUDE.md`** — all three now at a month or more unfixed, the longest-standing items this department tracks.
8. **Get this department real `Bash`/`Agent` access.** Fifth consecutive reporting cycle without it, now independently corroborated by this week's accounting report hitting the same gap on a session that expected it to work.
9. **Lower priority**: correct the `fragrance-store-quality-upgrade` campaign doc's stale download language (month-old, unfixed); record an explicit handoff for the comparison/review content unblock (Web Dev restores the routes, Content writes the first piece) — now roughly a month unclaimed; record a go/no-go on the 4 idle web templates and on Sales' `task 173`/`204` given they're both blocked on the same stalled content workstream.
