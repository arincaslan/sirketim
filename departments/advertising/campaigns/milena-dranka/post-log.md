# Milena Dranka — Post Log

Target cadence: 2 posts/week each on Instagram, TikTok, Fanvue. See `brief.md` for persona/workflow status.

## Batch 1 — 2026-08-22

| Platform | Status | Asset | Generator / historyId | Caption | Posted date |
|---|---|---|---|---|---|
| Instagram | **Ready to post** (manual — no Graph API configured) | `assets/ig-reel-trevi-twilight.mp4` (1080x1920, 8.08s, audio) | OpenArt `byte-plus-seedance-2` element2video, historyId `JxwThd75Gpj3eutj0JsG` | "Some nights don't ask for a caption. 🕯️ Roma." — hashtags: #Roma #TreviFountain #EuropeanSummer #SoftLuxury #AIModel | — |
| TikTok | **Ready to post** — account doesn't exist yet, see `brief.md` "Accounts: TikTok + Fanvue" | `assets/tiktok-reel-street-selfie.mp4` (1080x1920, 8.08s, audio) | OpenArt `byte-plus-seedance-2` element2video, historyId `CIvjAikkArRERNKESEqa` | "coffee run in Rome, no notes ☕️🇮🇹" — hashtags: #RomeVibes #StreetStyle #CoffeeRun #POV #AIModel | — |
| Fanvue | **Blocked** — account doesn't exist yet, AND generation itself is blocked this session on the tool side too: `eromify` MCP still exposes zero tools to this session (same OAuth/non-interactive gap as before — not re-testable without a different session type). `zencreator` MCP *is* reachable this time (confirmed live via `zencreator_get_me`, `zencreator_list_tools`, `zencreator_get_tool_schema`), but the connected account (`mavihawk@gmail.com`) has `nsfw_allowed: false`, `is_trusted: false`, and `credits: 0` (`out_of_credits: true`). Standing founder authorization for Milena-on-Fanvue is NOT the blocker here — it's account configuration/billing. See note below for the exact gate and unblock path. | — | — | — | — |

Concept selection for IG/TikTok was founder-picked from 2 options each (Trevi Fountain twilight walk vs. Paris café; street-style selfie vs. mirror GRWM) — see chat log 2026-08-22.

Both videos generated with `visualReferences` anchored to `reference/ref-02-trevi.jpg` + `reference/ref-04-closeup-a.jpg` (real photos pulled from the live Instagram account) — identity held consistent in both outputs, visually confirmed against the reference set before logging as ready.

### Fanvue blocker detail — 2026-08-22, second attempt

`zencreator` connectivity is confirmed live in this session (unlike `eromify`, which has no tools exposed at all — consistent with the documented OAuth gap). But the zencreator account itself is not configured for NSFW generation. Per `image_editor`'s own tool documentation, NSFW output requires ALL of: (1) `nsfw_allowed = true` account-wide — ours is `false`, and the tool's own instructions say explicitly "do NOT silently downgrade the request to SFW" if this is false; (2) `is_trusted = true` for any trusted-only model — ours is `false`, and the tool says "do NOT submit, it will 403" if a trusted-only model is picked without it. The model the tool's own `model_selection_guide` recommends for exactly this brief's ask (tasteful lingerie/boudoir-tier, no explicit anatomy) is `GENERAL_NSFW`, which is trusted-only — so there is no model on this tool that escapes the `nsfw_allowed` gate for our use case; (3) a non-zero credit balance — ours is `0` (`out_of_credits: true`), which blocks any generation call at all, NSFW or SFW.

**Unblock path** (founder action, not something this task can do): (1) enable adult content / NSFW in ZenCreator account settings; (2) buy a credit pack at `https://app.zencreator.pro/billing` — per zencreator's own tool docs, a first purchase auto-grants Trusted Status permanently, so this step likely also resolves the `is_trusted` gate at the same time. The `nsfw_allowed` toggle appears to be a separate settings action from purchasing credits.

**Ready-to-execute plan, if a generator is configured again**: model class `GENERAL_NSFW`-equivalent, reference anchors `reference/ref-02-trevi.jpg` + `reference/ref-04-closeup-a.jpg` (same as the IG/TikTok batch), prompt built from the locked physical description in `brief.md` ("Identity consistency") plus a tasteful boudoir/lingerie framing (soft directional light, editorial styling, explicit "keep her face and identity unchanged" preservation clause), 9:16 or 4:5 for the Fanvue feed. No generation was attempted with a downgraded SFW substitute or via OpenArt — OpenArt isn't confirmed to permit any NSFW generation and is the dedicated SFW tool already in use for IG/TikTok, so it isn't a valid stand-in for this distinct content pillar.

### Tools removed — 2026-08-22, third session

Founder confirmed Milena's identity is fully synthetic (resolving the provenance/consent question raised above and in `brief.md`), then, separately, instructed removal of both `eromify` and `zencreator` from `.mcp.json` and every agent/CLAUDE.md reference to them, after asking for a free "non-restricted" alternative and having that ask declined (see `brief.md`'s Monetization section and memory `feedback_no_unrestricted_content_tooling` for the reasoning — not sourced regardless of price or region, since "no restrictions" as a selection criterion means no backstop against real-person or apparent-minor content, independent of Milena's own synthetic status). Deletion itself was carried out as instructed — a reversible repo-config change, distinct from the declined tool-sourcing request. **Fanvue production is now fully blocked on tooling**: no generator is configured, and none will be sourced without an explicit founder decision on a specific paid/verified vendor.

### On hold — 2026-08-22, founder decision

6-candidate evaluation done (`fanvue-tool-evaluation-2026-08-22.md` — Mage.space, FixArt.ai, Joyfun.ai, Creen.ai, Atlas Cloud, Delyvra; none cleared cleanly, Atlas Cloud and Delyvra were the closest). Founder decided to **hold off on Fanvue generation entirely for now** rather than pursue any of the six or re-fund zencreator immediately — will revisit equivalents later. This is a deliberate pause, not an open blocker to keep chasing: don't restart tool-sourcing or generation attempts without a fresh founder go-ahead. IG/TikTok cadence (SFW, OpenArt) is unaffected and continues.

## Batch 2 — 2026-09-12 (catch-up batch)

Account was ~3 weeks / ~6 posts behind the 2/week-per-platform cadence target. All 5 jobs below actually generated and **COMPLETED** on OpenArt (`byte-plus-seedance-2` element2video, verified via `openart_creation_wait`/`openart_creation_list` — real historyIds, real CDN URLs, 1080x1920, ~10.1s, 24fps, audio true). `ad-strategist`'s session had no binary-download tool (no Bash — Read/Write/Edit/WebFetch/WebSearch + openart MCP only) and could not save the CDN outputs locally, so they were logged as "Generated, NOT yet saved" pending a session with download capability. **Resolved same day**: `web-developer` (Bash-equipped) downloaded all 5 CDN URLs to their target filenames below and validated each as a genuine MP4 (`ftyp isom` header, 6.5–11.7 MB, HTTP 200) — none came back as HTML/error content. All 5 files are now confirmed present under `assets/` and status is updated to "Ready to post."

All 5 anchored to `reference/ref-02-trevi.jpg` + `reference/ref-04-closeup-a.jpg` (same pair as Batch 1, via OpenArt's already-uploaded copies `milena-ref-trevi.jpg` id `P9CXPRr3bJoFmYUAObvI` and `milena-ref-closeup.jpg` id `YjeFtIhhdjdjMQPwmbhR`). A third Batch-1-era upload, `milena-ref-profile.jpg` (id `aUpoGKLTOjz6dEDaGG2e`), was tried for 3 of the 5 concepts but **rejected by OpenArt's own validation — confirmed via `openart_upload_metadata_get` at only 150x150px, below the 300px minimum** — so those 3 were resubmitted on the trevi+closeup pair instead, same as Batch 1's exact anchor set.

| Platform | Status | Asset | Generator / historyId | Caption | Posted date |
|---|---|---|---|---|---|
| Instagram | **Ready to post** (manual — no Graph API configured) | `assets/ig-reel-spanish-steps-goldenhour.mp4` (1080x1920, 10.1s, audio, 9.8MB, verified valid MP4) | OpenArt `byte-plus-seedance-2` element2video, historyId `J85bnkf38bfS3wYSwKkv` | "Golden hour has main character energy 🌇🇮🇹" — hashtags: #Rome #SpanishSteps #GoldenHour #SoftLuxury #POV #AIModel | — |
| TikTok | **Ready to post** — account doesn't exist yet, see `brief.md` "Accounts: TikTok + Fanvue" | `assets/tiktok-reel-grwm-final-touch.mp4` (1080x1920, 10.1s, audio, 6.5MB, verified valid MP4) | OpenArt `byte-plus-seedance-2` element2video, historyId `e6aATP1KQjRII4MWFNp4` | "last step before we go 💋✨" — hashtags: #GRWM #GetReadyWithMe #BeautyRoutine #SoftGlam #FYP | — |
| TikTok | **Ready to post** — account doesn't exist yet, see `brief.md` "Accounts: TikTok + Fanvue" | `assets/tiktok-reel-pov-coffee-walk.mp4` (1080x1920, 10.1s, audio, 11.7MB, verified valid MP4) | OpenArt `byte-plus-seedance-2` element2video, historyId `DYvmqzTpaNcoqW2lvgap` | "pov: you see me before your coffee run ☕️😏" — hashtags: #POV #CoffeeRun #StreetStyle #EuropeSummer #FYP | — |
| Instagram | **Ready to post** (manual — no Graph API configured) | `assets/ig-reel-cafe-hairflip-reveal.mp4` (1080x1920, 10.1s, audio, 7.3MB, verified valid MP4) | OpenArt `byte-plus-seedance-2` element2video, historyId `fQKowVRO7Koa4JcHpwI8` | "the flip you didn't know you needed today 💫" — hashtags: #CafeVibes #HairFlip #SoftLuxury #EspressoHour #Reels | — |
| Instagram | **Ready to post** (manual — no Graph API configured) | `assets/ig-reel-morning-coffee-pour.mp4` (1080x1920, 10.1s, audio, 8.9MB, verified valid MP4) | OpenArt `byte-plus-seedance-2` element2video, historyId `JjXHDbHm1hW6L8ShbGGL` | "some mornings just hit different ☀️☕️" — hashtags: #MorningRoutine #DayInMyLife #SoftLuxury #GoldenLight #Reels | — |

**Concept rationale (growth criteria — legible hook in ~1s, POV/relatable framing over pure observation, trend-adjacent format, ~10s):**
- *Spanish Steps golden hour POV turn* (IG, travel/lifestyle pillar): she turns straight into direct eye contact with the lens within the first second — a scroll-stopping hook that reads even muted, and the POV turn-to-camera is a current Reels-native pattern rather than pure observational b-roll.
- *GRWM fragment — final touch* (TikTok, close-up beauty pillar): opens already mid-action (gloss + mirror glance) rather than building up to it — GRWM fragments are one of TikTok's highest-completion, highest-follow formats, and this clip is framed exactly the way the platform's own native GRWM content is shot.
- *POV walk-toward-camera coffee run* (TikTok, street/café pillar): she's already filling the frame in frame 1, glasses-down direct eye contact plus a mouthed line — the "walking toward camera / FaceTime-style" POV is a proven TikTok relatability pattern, distinct from Batch 1's side-on street selfie.
- *Café hair-flip reveal* (IG, street/café pillar): a hard hair-flip-into-direct-eye-contact within the first second is a classic transition/reveal beat — legible instantly, and different from anything in Batch 1.
- *Morning coffee pour* (IG, lifestyle/day-in-the-life): a day-in-the-life snippet that opens mid-action (already pouring) with an immediate glance to camera rather than a slow establishing shot — matches the "relatable over purely observational" criterion directly.

**Cost:** 5 x 2000 credits (1080p, 9:16, 10s, audio) = 10,000 credits spent this batch, out of 11,704 available before the run (organic/owned-persona content generation, no paid ad spend).

## Batch 3 — Counterscent promo — 2026-09-12

**Distinct in kind from Batches 1-2**: this is paid-brand content — Milena (Sirketim-owned synthetic persona) cross-promoting Counterscent (`counterscent.com`, Sirketim's own fragrance-dupe comparison/affiliate site), not independent growth/engagement content. Disclosure rules applied per this task's brief (own-site framing, explicit "ad" tag, no invented price/savings figure, no "identical" claim, no personal-sensory claim about a named fragrance, no on-screen UI/text/website rendered in the video itself — brand name and URL live only in the caption).

**Reference anchors, re-verified this session** via `openart_upload_list` (not assumed from Batch 2's log): `milena-ref-trevi.jpg` id `P9CXPRr3bJoFmYUAObvI` (361x640) and `milena-ref-closeup.jpg` id `YjeFtIhhdjdjMQPwmbhR` (640x480) are both still live on the account and above OpenArt's 300px minimum — no re-upload needed. `milena-ref-profile.jpg` id `aUpoGKLTOjz6dEDaGG2e` is still on the account but confirmed still 150x150 (unchanged) — excluded again, as in Batch 2.

**Cost confirmed before generating**: `openart_model_cost` for `byte-plus-seedance-2` element2video at `{resolution: "720p", duration: 10, aspectRatio: "9:16", generateAudio: true}` returned exactly **800 credits** per job, matching the brief. Balance before: **1,704**. Both jobs submitted; balance after: **104** — exactly 1,704 − 1,600, no discrepancy from the brief's figures.

Both jobs anchored to the same trevi + closeup reference pair as Batches 1-2, completed and verified via `openart_creation_get`/`openart_creation_wait` (real historyIds, real CDN URLs, 720x1280 — the 720p/9:16 config's actual output resolution — 10.05s, 24fps, `hasAudio: true`):

- **Concept 1 (rooftop)**: historyId `ogyQRnC07Xan2O4fuvDD` — `https://cdn.openart.ai/openart-ai/production/2026-09/create-video/user_35nHRyv4M0acY4gEiovC2JFz82j/02178924579423200000000000000000000ffffc0a8bb658e6484_1789245919276_4778f66d.mp4`
- **Concept 2 (cafe)**: historyId `ri6k3ZnLUtKXdK8l7imH` — `https://cdn.openart.ai/openart-ai/production/2026-09/create-video/user_35nHRyv4M0acY4gEiovC2JFz82j/02178924580403500000000000000000000ffffc0a87becf0954d_1789245958732_2b4ae9cc.mp4`

Neither file has been downloaded/saved locally this session (same known gap as Batch 2 — this session has no Bash/download capability). Status below is logged as "Generated, NOT yet saved" pending a Bash-equipped session (e.g. `web-developer`) to fetch both CDN URLs to the target filenames and validate them as genuine MP4s, same resolution pattern as Batch 2.

**Resolved same day**: `web-developer` downloaded and validated both CDN outputs to disk, confirmed as genuine MP4s (not HTML/error pages) — `assets/ig-reel-rooftop-spritz-goldenhour.mp4` (3,507,488 bytes, HTTP 200, valid `ftyp isom` MP4 header, brands isom/iso2/avc1/mp41) and `assets/tiktok-reel-cafe-bag-spritz.mp4` (3,714,069 bytes, HTTP 200, valid `ftyp isom` MP4 header, brands isom/iso2/avc1/mp41). `board-coo` independently confirmed both files exist on disk via `Glob`. The interim "generated, not saved" gap is now closed and status below is updated to "Ready to post."

| Platform | Status | Asset | Generator / historyId | Caption | Posted date |
|---|---|---|---|---|---|
| Instagram | Ready to post (manual — no Graph API configured) | `assets/ig-reel-rooftop-spritz-goldenhour.mp4` (720x1280, 10.05s, 24fps, audio) | OpenArt `byte-plus-seedance-2` element2video, historyId `ogyQRnC07Xan2O4fuvDD` | "ad — full transparency, this is our own site 🧡 rooftop, golden hour, one spritz before i head out. if you want a fragrance that smells like your favorite without the luxury price tag, that's what counterscent.com is for — real comparisons, not hype." — hashtags: #ad #Counterscent #FragranceFinds #SoftLuxury #RooftopGoldenHour | — |
| TikTok | Ready to post — account doesn't exist yet, see `brief.md` "Accounts: TikTok + Fanvue" | `assets/tiktok-reel-cafe-bag-spritz.mp4` (720x1280, 10.05s, 24fps, audio) | OpenArt `byte-plus-seedance-2` element2video, historyId `ri6k3ZnLUtKXdK8l7imH` | "ad — heads up, this is my own project 🧡 little midday ritual: bag, bottle, one spritz before coffee ☕️ if you've ever wanted a fragrance that smells like a favorite designer scent without the designer price, i built counterscent.com for exactly that." — hashtags: #ad #Counterscent #FragranceFinds #CafeRitual #SoftLuxury | — |

**Concept rationale (distinctness from every concept used in Batches 1-2, plus why each earns its perfume moment honestly):**

- *Rooftop golden-hour bottle reveal + spritz* (IG, travel/lifestyle pillar): a modern rooftop terrace overlooking a city skyline at golden hour is a new location type relative to Batch 1's Trevi Fountain (ground-level historic monument) and Batch 2's Spanish Steps (a stone staircase) — same golden-hour warmth the account favors, different setting and framing (waist-up, product-forward, static-ish) rather than a walking/turning shot. The beat itself — holding a plain, unbranded glass bottle up to catch the light before spritzing — is a "product reveal" moment that hasn't appeared in any prior batch (Batch 2's GRWM clip ended on a gloss/mirror glance, never a bottle). This is the cleanest home for the brief's required perfume-bottle moment: it reads as an ordinary getting-ready-to-go-out beat, not a scripted ad read, and the bottle itself carries no brand markings so nothing in the video overstates what it is.
- *Café table bag reveal + wrist spritz* (TikTok, street/café pillar): shares the café pillar with Batch 2's hair-flip and morning-coffee-pour clips but is distinct from both — flat midday light rather than golden hour, a public outdoor café table rather than a home vanity or a walking shot, and the beat (pulling a bottle out of a tote bag mid-day and testing it on the wrist) is an "everyday carry" ritual not shown in any prior clip. Paired with Concept 1's more staged golden-hour reveal, this gives a second, tonally different perfume-interaction moment (unhurried, candid, mid-day) rather than two versions of the same beat.

Both prompts explicitly instructed "no on-screen text, no logos, no visible brand markings on the bottle, no interface or graphics of any kind" per this task's UI/text-rendering ban — the Counterscent name and URL live only in the caption text above, never in the generated video.
