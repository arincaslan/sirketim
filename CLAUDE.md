# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Sirketim is a solo-founder, multi-department agency workspace, not a single codebase. Claude Code subagents act as each department's actual workers (drafting, research, first-pass production), not just copilots; the founder reviews and decides. Eight departments: web development, architecture (real building/interior design), sales, advertising, communication, content (long-form/affiliate content production), accounting, control/audit. A three-member board (COO, CFO, Controller) sits between the founder and the departments — see `shared/board.md`.

## Commands

Every Next.js project (client or product) is self-contained, each with its own `package.json` scoped to its own subfolder. **For development, `cd` into the specific project before running anything** — there is still no lint or test at the repo root, and no workspace/monorepo tooling.

**The one exception, added 2026-08-27: a deploy shim.** The repo root has a `package.json` with **two** scripts — `build`, which `cd`s into `products/affiliate-sites/fragrance-dupes` and builds it, and `postinstall`, which runs `scripts/ci-postinstall.mjs` — plus a root `wrangler.jsonc` pointing at that project's `out/`. This exists solely because Cloudflare runs `npx wrangler deploy` from the repo root when a project has no "root directory" set; **four** builds failed before it worked, first with *"Could not detect a directory containing static files"* and then because Cloudflare never ran the build command at all. `postinstall` is the load-bearing half and the least obvious: it is gated on `WORKERS_CI`/`CF_PAGES`/`CI` and exists because Cloudflare's automatic dependency install is the only hook that reliably fires. There is also a root `package-lock.json` for one reason — without it Cloudflare picks **bun**, which reports *"No packages! Deleted empty lockfile"* and skips the build.

**Verifying a change to any of this: `npm run build` at the root is NOT what Cloudflare runs**, and passing it proves little. The real rehearsal is `rm -rf out && WORKERS_CI=1 npm install`, which exercises the `postinstall` path. This was learned the expensive way — two "fixes" were verified with the wrong command and shipped two more failed builds.

It is **not** a workspace root, has no dependencies, and must not grow into a build system: a second deployable site gets its own Cloudflare project with a root directory configured, not a competing root config. See `departments/web-development/CLAUDE.md` for the deploy settings.

**The site went live on Cloudflare Workers on 2026-08-27** — $0/mo, HTTPS valid, mail on Hostinger with DKIM verified, GA4 `G-4Q54ZJKVW1` collecting (cookieless — see below), Search Console verified with the sitemap submitted.

**It launched as `parfumoza.com` and was moved to `counterscent.com` the same day** — the move is **complete**, not in flight: nameservers are Cloudflare's (`michelle`/`mike.ns.cloudflare.com`), the apex serves 200 from Cloudflare, the Worker is renamed `counterscent`, and GA4 + Search Console were re-pointed. Re-verified 2026-08-27. The rename happened because the first name collided with **Parfumo.com** (a 227,000-fragrance database operating since 2008) and **Parfumado.com**. The full reasoning, and the name-vetting rule that came out of it, is in `products/affiliate-sites/fragrance-dupes/CLAUDE.md` — read it before proposing any brand name. **Anything below that still names the old domain is describing the pre-move state, deliberately; do not "fix" it with a find-and-replace.** One loose end remains, and it is a founder action, not a code one: `parfumoza.com` is still attached to the Cloudflare account and to the Worker's Domains & Routes, and needs removing from both.

**The zone's full record set — MX/SPF/DKIM/DMARC included — is in `departments/web-development/counterscent-dns-migration.md`**, together with the trap that a proxied CNAME breaks DKIM signing. Go there before touching DNS; the records are not reconstructable from the Cloudflare dashboard alone once deleted.

**GA4 here is deliberately cookieless** (`client_storage: 'none'` + `anonymize_ip` in `components/kit/Analytics.tsx`), which is why the site carries no consent banner under KVKK/ePrivacy. **The measurement caveat matters more than the config**: without a client ID, GA4 cannot recognise a returning visitor, so *Users* inflates toward *Sessions*. That is the direction that flatters us — so **do not quote GA4 Users on an affiliate-network application**; use Cloudflare Web Analytics for a defensible visitor count.

A recurring false alarm worth knowing before you debug: the founder's **OpenWrt router at `192.168.1.1` repeatedly served a stale apex A record for `parfumoza.com` pointing at `2.57.91.91`**, Hostinger's old parked-page IP, which returns a real HTTP 200 carrying `<title>Parked Domain name on Hostinger DNS system</title>` — so a browser shows a parked page that looks exactly like a broken deploy. `Clear-DnsClientCache` does not help; the router re-serves it. The tell is that **`www` resolved correctly through the same router while the apex did not.** It produced three separate "the site is down" reports for a site that was live worldwide each time. **Never declare a site here up or down from one local `curl`** — force-resolve it (`curl --resolve <domain>:443:<cloudflare-ip>`), query `1.1.1.1`/`8.8.8.8`, and check the authoritative nameservers directly. The opposite mistake has also been made in this repo: reporting the site *down* on the strength of a single local lookup.

Real projects exist today under `products/web-templates/`: `agency-landing/`, and three deliberately distinct fragrance e-commerce templates — `fragrance-store/` (warm cream/gold, rounded), `fragrance-store-2/` ("Nocturne" — dark obsidian/ember-red, sharp corners), `fragrance-store-3/` ("LUMEN" — cool mineral-stone/warm ivory, electric-lavender accent). Each new template's design direction is checked against the others' actual palette/typography before committing, specifically to avoid a third re-skin. Plus `products/affiliate-sites/fragrance-dupes/` (see its own CLAUDE.md). Inside any of them: `npm install`, then `npm run dev` / `npm run build` / `npm run lint`. No test script is configured in any project yet — don't invent one; verify by running the app and asserting against real output. The same self-contained pattern applies once the `new-web-client` skill scaffolds a client under `departments/web-development/clients/<slug>/`.

**Never run `npm run build` while `npm run dev` is live in the same project directory.** Both write `.next/` and the collision corrupts the webpack manifest badly enough that the dev server starts 404ing every route. Recovery: kill both processes, `rm -rf .next`, then run one at a time. Documented for real in `products/affiliate-sites/fragrance-dupes/README.md` after it happened there, but it applies to every Next.js project in this repo.

Setting the workspace up on a different machine (tool installs, `gh`/Claude Code auth, MCP approval): see `SETUP.md`.

Commands that are already real and used in this repo:
- **Architecture CAD generation**: Python with `ezdxf` (DXF) and `svgwrite` (SVG) — already installed system-wide, driven through the department's own `departments/architecture/lib/cadgen/` library (data model + generator + compliance checker), not freehanded per project. See `departments/architecture/CLAUDE.md`.
- **Architecture DWG conversion**: ODA File Converter, run headless — `"<install path>\ODAFileConverter.exe" <inputFolder> <outputFolder> <outVer> <outType> <recurse:0/1> <audit:0/1>`. Installed per-user (machine-wide MSI install needs admin rights this environment doesn't have); `ezdxf`'s `odafc` addon needs its `win_exec_path` pointed at the real per-user install path or it silently looks in `Program Files` instead — see `departments/architecture/CLAUDE.md` and `departments/architecture/reports/cad-tooling-gap-analysis.md` for the exact path and gotcha.
- **Scanned-PDF reading**: Poppler (`pdftoppm`/`pdftotext`/etc., via winget) — required for the Read tool to render scanned/image-only PDF pages; without it, scanned intake documents (common in architecture zoning intake) fail to read. Installed system-wide.
- **Architecture 3D rendering**: Blender 4.5 LTS, run headless — `"C:\Program Files\Blender Foundation\Blender 4.5\blender.exe" --background --python <script>.py`. Installed via winget (`BlenderFoundation.Blender.LTS.4.5`), added to the user PATH. Note: `bpy` must be imported explicitly in the script; it isn't auto-imported.
- **MCP connector status**: `claude mcp list` shows configured servers; `/mcp` inside an interactive session approves a pending one. `.mcp.json` configures eight — four listed here plus four Hostinger servers (`hostinger-domains`, `hostinger-dns`, `hostinger-hosting`, `hostinger-vps`) added 2026-08-27 for the `counterscent.com` launch, whose `HOSTINGER_API_TOKEN` is a per-machine env var never committed, and whose setup gotchas (a same-session `setx` reads back empty and looks exactly like a 401; aggressive 429 rate limits) are documented in `departments/web-development/CLAUDE.md`. Hostinger's `billing`, `ecommerce` and `reach` servers were deliberately left out — billing exposes purchase operations. The four original servers: `openart` (OAuth, no API key), `21st` (21st.dev's Magic MCP — component search/generation for web-development, granted to `web-developer` as `mcp__21st`; needs `TWENTY_FIRST_API_KEY` set as a real local environment variable, referenced from `.mcp.json` as `${TWENTY_FIRST_API_KEY}` rather than committed — see `departments/web-development/CLAUDE.md`), `chrome-devtools` (Google's Chrome DevTools MCP, free, no API key, runs via `npx chrome-devtools-mcp@latest --isolated` — drives a real local Chrome instance for screenshots/console/network/perf, granted to `web-developer` as `mcp__chrome-devtools`), and `fanvue` (`https://mcp.fanvue.com/mcp`, added 2026-08-22, granted only to `ad-strategist`) — Fanvue's own official account/posting server (publish/schedule posts, analytics, vault, fan chat), unusable until a real KYC-verified Fanvue account exists, and gated separately from generation: publishing/reading data rides the founder's standing per-persona/platform authorization, but replying to fans or sending mass messages needs its own explicit founder go-ahead (same posture as Communication's manual-send policy) — see `departments/advertising/CLAUDE.md` "Fanvue account/posting". **Two AI-influencer content generators, `eromify` and `zencreator`, were configured 2026-08-22 and then REMOVED the same day** at the founder's direct, repeated instruction, after neither proved usable for free and a request to source a free "unrestricted" replacement was declined (see `departments/advertising/CLAUDE.md` "AI-influencer content" and memory `feedback_no_unrestricted_content_tooling` for the full reasoning — that reasoning holds regardless of price or where a tool is sourced from). No Fanvue-capable (+18) content generator is configured as of 2026-08-22; OpenArt was checked directly and confirmed incapable. Approval appears to be scoped per session/process, not global — a session can show `⏸ Pending approval` even after the founder approved it elsewhere, and a long-running session started *before* an env var was set (or before a server was added to `.mcp.json`) won't see it until a fresh session starts. **`claude mcp list` itself is not fully reliable** — confirmed 2026-08-21: it reported `openart` as pending in a stale session while a direct `mcp__openart__openart_account_get` call succeeded immediately (real account/credits returned). So don't stop at `claude mcp list`: if a connector matters for the task at hand, try calling one of its actual tools (or `ToolSearch` for `mcp__<server>__*`) — if the tool genuinely isn't in your available set, that's the reliable signal of real unavailability, not what the list command printed. This applies doubly to background subagents: they don't automatically inherit a fresh top-level session's connections, so verify independently inside the subagent rather than assuming a working connector up top means it works everywhere.
- **Third-party skill installs and the `.agents/skills/` directory**: `.claude/skills/` mostly holds skills authored for this repo (`new-web-client`, `project-brief`, etc.), but three external skill packages are now installed for the web-development department: `ui-ux-pro-max` (design-intelligence data, installed via `uipro init --ai claude`, npm package `ui-ux-pro-max-cli`) sits directly under `.claude/skills/ui-ux-pro-max/`; Emil Kowalski's animation/design skill set and `design-taste-frontend` (the "taste" skill, `Leonxlnx/taste-skill`) are both installed via `npx skills@latest add <repo>` (the latter with `--skill design-taste-frontend` to avoid its repo's 12 other bundled style/output variants) and land in a top-level `.agents/skills/` directory with `.claude/skills/<name>` symlinks pointing into it — that split is the installer's own convention (`.agents/skills/` is a cross-tool location it also symlinks into other agent CLIs' skill dirs), not something to fight. The `npx skills@latest add` installs (but not `ui-ux-pro-max`) are tracked in root-level `skills-lock.json` (source repo, path, content hash per skill) — check it before assuming a given skill's provenance or re-running an install. **Known gotcha**: `ui-ux-pro-max`'s installer bundles several other skills (`design`, `brand`, `banner-design`, `design-system`, `slides`, `ui-styling`) alongside it; its `design` skill has the same name as Anthropic's own built-in `design` skill (the Claude Design canvas one `internal/dashboard/` depends on) and shadows it if left installed. These six were deliberately removed after install — if `uipro init`/`uipro update` is ever re-run, delete them again rather than letting the shadow reappear. **Confirmed per-machine gap, 2026-08-25**: at a fresh session's start on this machine, `.agents/skills/`, the `.claude/skills/<name>` symlinks for Emil's set and `design-taste-frontend`, and `skills-lock.json` itself were all absent — despite this doc's unconditional claim that they're installed — because `npx skills@latest add` writes to locations `.gitignore` deliberately excludes (`.agents/`, plus each individual `.claude/skills/<name>/` symlink), so none of it travels via `git clone`/`pull` the way the rest of this repo does. A `web-developer` build hit this mid-task and self-healed by re-running the installs. Same class of problem as the "Two physical machines" gotcha below, different mechanism (gitignored-by-design, not a wrong hardcoded path) — don't trust this doc's "installed" claim alone; if a session needs one of these two skills and `.claude/skills/<name>/` isn't actually there, re-run `npx skills@latest add <repo>` (with `--skill design-taste-frontend` for that one) on the machine/environment you're actually on. **Current state, verified 2026-08-27 on the `Semih` machine**: all of it is present — 13 skills under `.agents/skills/`, their `.claude/skills/<name>` symlinks, `ui-ux-pro-max` as a real directory, `skills-lock.json` at the root, and none of the six shadowing skills. That says nothing about the `win10` machine, which is the whole point of the gotcha: this is per-machine state that git does not carry, so it is a thing to *check*, never a thing to inherit from this paragraph.
- **Git remote**: this repo pushes to `https://github.com/arincaslan/sirketim` (GitHub CLI `gh` is installed and authenticated as `arincaslan`). The repo is **public**, not private — a deliberate tradeoff made to unblock claude.ai's GitHub connector (it only had public-repo scope), so treat the ledger and every department's strategy docs as publicly visible. Two claude.ai scheduled routines (`Sirketim Weekly Accounting Report`, `Sirketim Weekly Control/Audit Report`) run weekly against `main` via `board-cfo`/`board-controller`, writing into `departments/accounting/reports/` and `departments/control/reports/` and pushing directly — a `git pull` may surface new files you didn't create locally.
- **Cross-session sync (do this first, every session)**: the founder works from multiple Claude Code surfaces (this CLI, claude.ai web/mobile). Board subagents (`board-coo`, `board-cfo`, `board-controller`) have no Bash/git tool access — they only ever see whatever is already sitting in the local working tree's files when they're invoked. They cannot discover work that landed on a remote branch. So at the start of a session, and before answering any "what's going on" / status question or delegating to the board, run `git fetch origin` and check `git branch -r` plus `git log main..origin/<branch>` for real work sitting on unmerged branches (commonly named `claude/*` from other sessions). Surface or merge what you find before trusting the board's picture of current state — otherwise their report will be stale by exactly however much cross-session work hasn't synced to `main` yet.
- **Two physical machines, two different Windows usernames — a real, recurring path gotcha**: the founder works from two computers, one logged in as Windows user `win10`, the other as `Semih`. This repo is machine-portable (it's just git), but any **absolute local path this repo's code or docs hardcode** (`C:\Users\<name>\...`) is NOT portable across the two — it's silently wrong on whichever machine didn't write it. This already happened for real: `departments/architecture/lib/cadgen/export_dwg.py`'s `ODA_EXEC_PATH` was written against a `C:\Users\Semih\...` install and broke on the `win10` machine until caught and fixed (see `departments/architecture/CLAUDE.md`'s DWG-conversion gotcha and `departments/architecture/reports/cad-tooling-gap-analysis.md`). After any `git clone`/`git pull` on either machine, and before trusting a hardcoded local install path anywhere in this repo (Python scripts, department CLAUDE.md snippets, anything under `C:\Users\...`), check whether it matches `$env:USERNAME`/`whoami` on the machine you're actually on — if it names the other user, re-verify the real local install path on this machine and update the code/doc, don't assume it still resolves. This applies to any future per-user tool install (not just ODA File Converter) — machine-wide/system installs avoid the problem entirely when available; per-user installs need this check.
- **Before building on any payment, payout, or affiliate provider, check `departments/accounting/CLAUDE.md` first.** Sirketim is a **Turkey-based A.Ş.**, and Turkey is excluded from several providers most stacks treat as defaults. Confirmed unusable: **Stripe** (verified 2026-08-26 against `stripe.com/global` — Turkey absent from ~50 countries, no waitlist), **PayPal** (not operating in Turkey), **Gumroad** (its only two payout rails are PayPal and Stripe Connect). Confirmed usable: **Paddle** (merchant of record, ~10.2% all-in at low volume — a flat ~$15 wire fee dominates), **iyzico**/**PayTR** (Turkish, TRY, both support recurring), **Payoneer** (the rail for USD/GBP/EUR affiliate income, and the documented workaround for networks that cannot pay a TRY account). This is a **real mistake that already happened**: a full Stripe checkout + webhook integration was built for `products/affiliate-sites/fragrance-dupes/` in 2026-08-26 *after* this repo had already documented (2026-08-24) that Stripe does not serve Turkey — the repo knew and the assumption was never checked against it. The cost was recoverable only because no database existed yet. Same failure class as the hardcoded-path gotcha above: the answer was already written down, in another department's file.
- **Know which subagent has web access before delegating research.** `board-cfo`, `board-coo`, `board-controller`, `accountant` and `auditor` have **no `WebSearch`/`WebFetch`** — they can only reason from their training cutoff plus what is already on disk. Delegating "research provider X" to them yields confident, well-structured output whose provider facts may be months stale. This is not hypothetical: `departments/accounting/reports/payment-rails-investigation.md` correctly flagged **9 items it could not verify**, four of which the top-level session then checked live — and **two findings changed** (Amazon direct deposit to a Turkish bank turned out to be genuinely unavailable; Paddle turned out to be genuinely available). Handle it by having the agent flag unverifiable claims explicitly, then verifying them from the top level (which does have web access) before anyone builds on them. Also beware the summarizer inside `WebFetch`: one fetch this session claimed Turkey was eligible for EUR payouts "through the Eurozone programme", which is simply false — sanity-check a fetched answer against what you already know before recording it as fact.

## Architecture

CLAUDE.md files nest and each layer adds scope, not a replacement: this root file -> `departments/<dept>/CLAUDE.md` -> `departments/<dept>/clients/<slug>/CLAUDE.md` (created per-client by a skill), and for one product `products/affiliate-sites/fragrance-dupes/CLAUDE.md`. Read the relevant chain before doing department or client work, don't rely on the root file alone.

- **Subagents** (`.claude/agents/`) map 1:1 to departments — `web-developer`, `architecture-assistant`, `sales-strategist`, `ad-strategist`, `communication-strategist`, `content-strategist`, `accountant`, `auditor`. Each is scoped to its department's CLAUDE.md, has only the tools that department needs, and should be invoked for department-specific work instead of doing it ad hoc from root.
- **Verify a subagent's claimed output before reporting it as done.** A subagent's summary describes what it *intended* to do, which is not always what it did. Confirmed here 2026-08-26: `board-coo` reported writing a specific plan doc and delegating sections to three department subagents, in convincing detail — and no file existed anywhere (`Glob` for the path, `git status`, no trace of the delegated runs). The risk concentrates in **multi-layer delegation** (top level -> board member -> department subagents), because the intermediate layer's own report is the only thing coming back up. After any agent claims to have written a file, check it independently (`Glob` the path, `git status --short`, or `Read` it back) before relaying success or building on top of it. For a single cohesive document, writing it directly is often the better trade than another delegation layer. Subagents that *did* verify their own writes in the same session (`communication-strategist`, `ad-strategist`, both explicitly instructed to read the file back) produced real, correct output — so the instruction to self-verify is worth including in the prompt. **The stronger fix, proven 2026-08-26: run the leaf subagents read-only and text-return-only.** Briefed that way, `board-coo` had `web-developer` and `communication-strategist` return their sections as text without writing anything, then verified their load-bearing claims against the real files itself (8 of 8 confirmed) and reported honestly that no file existed yet. Nothing can be fabricated as "written" when nothing was supposed to be written, and the intermediate layer becomes a checker instead of an unverifiable relay. Prefer this shape whenever the deliverable is one synthesized document rather than parallel edits to separate files.
- **Board members** (`.claude/agents/board-*.md`) are a layer above departments, not an 8th/9th department — `board-coo` (owns web development, architecture, sales, advertising, communication, content), `board-cfo` (owns accounting), `board-controller` (owns control/audit). The founder briefs a board member with an outcome; it delegates to the department subagent(s) it owns via the Agent tool and reports back a synthesized result. See `shared/board.md`. Prefer addressing the board member over the department subagent directly when the request is an outcome rather than a specific department task.
- **Skills** (`.claude/skills/`) are the repeatable intake-to-deliverable workflows: `new-web-client`, `project-brief` (architecture), `client-proposal` (sales), `campaign-brief` (advertising), `product-listing` (sales/products), `affiliate-content` (content — niche → content plan → long-form draft → SEO/schema metadata, set up 2026-08-24 for the affiliate-sites initiative). Prefer these over freeform work when a task matches one.
- **`products/`** vs **`departments/<dept>/clients/`**: products are Sirketim's own sellable goods (web templates, architecture plan packs, POD prints), built by whichever department creates the underlying asset but sold by sales via marketplaces — separate from client-commissioned work. See `products/README.md` for the line breakdown.
- **`products/affiliate-sites/fragrance-dupes/` ("COUNTERSCENT") is the most complex codebase in this repo and the only one with its own `CLAUDE.md` — read it before touching that project.** It began as an affiliate content site and is mid-pivot into a two-sided marketplace (producers pay to list dupes; the founder approves each submission; revenue from subscriptions plus commission on dupes *and* originals). It is consequently the first project here to carry a **backend** — though as of 2026-08-27 what survives is `prisma/schema.prisma` (Postgres, not migrated) and Auth.js models; the Stripe checkout/webhook routes were **deleted**, and `app/api/` no longer exists. Planning lives in its `MARKETPLACE-PLAN.md`, `PRODUCER-PROGRAM.md`, and — as of 2026-08-26 — **`FINALIZATION-GUIDE.md`, which is the current ordered roadmap and the right place to start** (6 phases, department owners, costs, dependencies; also published as a dashboard-linked Artifact). Its non-obvious invariant: the displayed match score is *not* the raw computed one (a producer copying the original's notes verbatim would otherwise score exactly 100%) — see that project's CLAUDE.md before changing anything scoring-related.

  **Phases 0, 1 and 2 are complete as of 2026-08-27** — safe to show, online, and findable, with 12 content pieces (~12k words) and an 89-URL sitemap. **Nothing in Phase 3 is blocked by build work any more**, which is the single most useful thing to know before planning work here. It is blocked by two things a coding session cannot move: the founder's **company USD (*döviz*) account**, being opened 28 Aug — the only USD account on record is *personal*, which would make the invoice issuer and the payee different legal persons — and the **2–4 week analytics window** that started when GA4 began collecting on 27 Aug. Do not enrol with any affiliate network before that account exists; the payee named on an application is hard to change afterwards.

  It also has a **producer subscription surface** (`/producers`, `/producers/pricing`, `/producers/login`, and a server-gated `/producers/submit`), 68 reference fragrances across 8 houses, and a provider-agnostic `PaymentProvider` enum in its schema. Three things about it are counter-intuitive enough to state here:

  - **The catalog is real but the listings are empty on purpose.** `DUPES`, `REVIEWS` and `affiliateLinks` were all emptied on 2026-08-27 after the listed products turned out not to exist and the reviews turned out to be invented testimonials about real, named companies. Every empty state renders correctly; **do not hand-write entries back in.**
  - **`/producers/submit` is unreachable in production for anyone at all** — its gate requires `NODE_ENV === "development"`, which the bundler inlines at build time, so every discussion of producer tiers is downstream of that. Deliberately unreachable rather than insecure, but don't plan around it being live.
  - **The site has no dynamic server surface at all** — no `cookies()`/`headers()`/`force-dynamic`, and as of 2026-08-27 **zero route handlers**. `/go/[slug]/route.ts` was deleted in the static-export migration (a route handler cannot return a 302 in an export); the affiliate chokepoint is now `scripts/generate-redirects.mjs`, wired as the `prebuild` npm script so it writes `public/_redirects` before every export. Treat "no dynamic server surface" as an **invariant to preserve**, not an observation — adding a route handler or a server-read `searchParams` breaks the deploy, not the review.

    **This does not mean the site has no dynamic-segment URLs**, and reading it that way would repeat a mistake this repo already made: `/fragrance/[slug]` and `/guide/[slug]` both exist and both ship real pages, statically generated through `generateStaticParams` at build time. The "zero indexable URLs" gap the finalization plan opened on was closed in Phase 2 — the sitemap is 89 URLs. Dynamic *segments* are fine; dynamic *rendering* is what breaks the export.
- **`internal/dashboard/`** — the founder's single-page task board + charts (real assigned/fulfilled tasks per department, financial chart from the ledger, department report links). There is no server, build, or `package.json` here — it's static. Two files, two different edit paths depending on which surface you're in:
  - `internal/dashboard/design/Main.dc.html` + `canvas.json` — a **Claude Design canvas**, editable/previewable through Claude Desktop's Design canvas editor, republished from there via its own Save flow. Treat this as the reference/documentation copy of the task list.
  - `internal/dashboard/design/sirketim-dashboard.html` — a **plain, dependency-free static HTML/CSS/JS file**, hand-written (not generated) 2026-08-24 after `Main.dc.html`'s normal re-seed step (`seed-canvas.mjs` + the `design` skill) proved unavailable from a CLI session — that tooling appears to be Desktop-only. This file is what's actually published to the live Artifact URL from a CLI session: edit it directly, then `Artifact` publish (action `publish`, same `file_path`, passing the existing artifact's `url` so it updates in place rather than forking a new one — see memory `reference_dashboard_artifact_url`, or `Artifact` action `list` if that's gone stale). It reimplements the same data/board with independently-scrolling status columns, clamped task notes with a working expand toggle, a live search box, department filter, and a light/dark theme toggle — genuinely interactive, not templated markup.
  - **These two files can drift** — updating one does not update the other. When a task changes, update both: add/edit the entry in `Main.dc.html`'s `realTasks` array (keeps the Desktop-editable canvas current) and mirror the same entry into `sirketim-dashboard.html`'s `TASKS` array (keeps the live published page current) — matching `id`s make the two easy to diff against each other. Every task assigned or completed in a session should be reflected in both before the turn ends, per the founder's standing instruction.
- **`shared/clients.md`** is the single cross-department client/lead registry (a client can span multiple departments). **`shared/board.md`** is the board charter (who's on it, what they own). **`shared/brand.md`** is Sirketim's own brand/voice, still a placeholder.
- **Connector status** lives in two places: `.mcp.json` (MCP servers, e.g. `openart`) and each department's CLAUDE.md "Recommended connectors" section (everything else, mostly not yet configured — check before assuming a connector is live).

## Departments

- [Web Development](departments/web-development/CLAUDE.md) — Next.js + Tailwind + shadcn/ui, GitHub. **Hosting has no standing default** — it is picked per project against that file's comparison table, and the one live site runs on Cloudflare Workers. Do not write "Vercel" back in as the default: **Vercel's Hobby tier forbids commercial use**, which disqualifies it for anything affiliate-monetised, and Pro is ~$20/mo/member.
- [Architecture](departments/architecture/CLAUDE.md) — briefs, zoning research, to-scale CAD floor plans (DXF/SVG/DWG) via a shared `lib/cadgen/` Python library, 3D catalog renders (Blender + AI polish). Never claims permit-ready/stamped output.
- [Sales](departments/sales/CLAUDE.md) — client pipeline/proposals, plus productizing department output for near-zero-cost marketplace sales (Etsy primary).
- [Advertising](departments/advertising/CLAUDE.md) — OpenArt-generated content, organic-first posting (Instagram/TikTok, always SFW) plus a founder-gated Fanvue (+18) channel for AI-persona social accounts — currently no generator configured for it (`eromify`/`zencreator` removed 2026-08-22), paid spend only when budgeted or revenue-funded.
- [Communication](departments/communication/CLAUDE.md) — direct DM/email/phone outreach for Sirketim's own products; hands off interested prospects to Sales to close. No sending connectors configured yet (drafts ready for manual send).
- [Content](departments/content/CLAUDE.md) — long-form, evergreen content production (buying guides, comparisons, reviews) for affiliate/SEO sites, distinct from Advertising's short-form social content. Set up 2026-08-24 to own the content-production capability the affiliate-sites initiative needed. **12 pieces are published and live on `counterscent.com`** as of 2026-08-27 — but still **zero live affiliate links**, so every piece carries `disclosure: false`, correctly. Two things about this department diverge from its own CLAUDE.md and are worth knowing before you work in it: its documented workflow says it *hands off drafts* rather than committing into a site's repo, yet all 12 pieces sit in `products/affiliate-sites/fragrance-dupes/content/`; and it can currently only produce `guide` pieces, because `content/schema.ts` requires a mandatory `affiliateLinkId` on every `comparison` and `review` — with no enrolled programme, writing one would mean inventing a product to point at.
- [Accounting](departments/accounting/CLAUDE.md) — manual ledger of income/expenses/subscriptions (no bank/Stripe integration yet), weekly financial report.
- [Control/Audit](departments/control/CLAUDE.md) — internal audit of the other departments: workflow correctness, registry hygiene, cross-department coordination, productivity/yield. Read-only over other departments' files; reports findings, doesn't fix them.

## Conventions

- Client work: `departments/<department>/clients/<slug>/`. Products: `products/<line>/<slug>/`. Slugs are lowercase-hyphenated.
- Register every client/lead in `shared/clients.md` as soon as they're qualified.
- Every transaction (income, expense, subscription charge) gets a row in `departments/accounting/ledger.md` when it happens, not retroactively at report time.
- Don't claim a connector, deploy, or post happened if it isn't actually configured — say what's missing instead.
- **The same honesty rule applies to shipped UI, not just to what you tell the founder.** A feature whose backing service doesn't exist yet should be built and then say so plainly at the point of use — an explicit "not live yet, nothing was saved" state, or an API route returning 503 with a reason. Never a fake success. `products/affiliate-sites/fragrance-dupes/` does this in several places (review form, producer submission form, Stripe routes); those states are requirements, not placeholders to tidy away. A form that silently discards what someone typed is the user-facing version of claiming a connector works when it doesn't.
- **Fixture and placeholder data must be labelled as such in the file that holds it**, with what would make it real. This repo's data is overwhelmingly illustrative right now (fragrance catalogs, producer listings, customer reviews, every affiliate destination), and unlabelled fixtures read as fact to the next session. Where fabricated data would be actively harmful if published — invented customer reviews, for instance — say that too.
- **These CLAUDE.md files go stale faster than the code, and they go stale *confidently*.** A `/init` pass on 2026-08-27 found six separate claims in `products/affiliate-sites/fragrance-dupes/CLAUDE.md` that described a tree that no longer existed — routes documented as absent had been built, a backend documented as "inert, returns 503" had been deleted outright, and three data arrays documented by their contents had been emptied. Each was written accurately and then simply not revisited. The risk is specific: a stale doc reads exactly like a current one, and a future session will plan around "there is no `/fragrance/[slug]`" without ever checking. So **when a change makes a documented claim false, update the doc in the same change** — and treat any concrete claim in these files (a file exists, a count, a route, "this returns 503") as a *hypothesis to verify* rather than a fact, especially before building on it. Grepping the actual tree takes seconds; the four department reports and this file's own history show how expensive the alternative is.
- **A label in the data file does not protect the UI that renders it.** `products/affiliate-sites/fragrance-dupes/lib/reviews.ts` carried an explicit header saying "Do not present these as genuine customer reviews on a live site" — and the component rendered those same six invented reviews with author names, star ratings, dates and an aggregate average, under a heading reading "Reviews from people who bought…", with no fixture label anywhere on screen. The warning was written and then not honoured by the code one file away. **When fixture data is about real, named third parties, it is a different category of risk than illustrative specs** — those six reviews name real operating companies and include a negative one, which is an FTC Fake Reviews Rule and trade-libel exposure rather than a tidiness problem. So: label the fixture in the file *and* make the rendered surface say it, or don't render it at all.
